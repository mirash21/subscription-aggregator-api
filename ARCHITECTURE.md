# 🏗️ Архитектура проекта

## 📐 Общая архитектура

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Client Apps   │────│  FastAPI Layer  │────│  PostgreSQL     │
│  (Web/Mobile)   │    │   (Business)    │    │   (Storage)     │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
              ┌──────────┐        ┌──────────┐
              │  Models  │        │ Schemas  │
              │ (SQLAlchemy)      │ (Pydantic) │
              └──────────┘        └──────────┘
                    │                   │
              ┌──────────┐        ┌──────────┐
              │ Database │        │  Logging │
              │  Layer   │        │  System  │
              └──────────┘        └──────────┘
```

## 📁 Структура проекта

```
subscription-aggregator-api/
├── app/                          # Основное приложение
│   ├── api/                     # API слой
│   │   └── routes/
│   │       └── subscriptions.py # REST endpoints
│   ├── database/                # Слой доступа к данным
│   │   └── session.py          # Database connection
│   ├── models/                  # Domain модели
│   │   └── subscription.py     # Subscription entity
│   ├── schemas/                 # DTO и валидация
│   │   └── subscription.py     # Pydantic models
│   ├── utils/                   # Утилиты
│   │   └── logger.py           # Логирование
│   ├── main.py                 # Application entry point
│   └── __init__.py
├── alembic/                     # Database migrations
├── migrations/                  # Raw SQL migrations
├── tests/                       # Automated tests
├── docker-compose.yml           # Container orchestration
├── Dockerfile                   # Application container
├── requirements.txt             # Dependencies
└── docs/                        # Documentation
```

## 🏗️ Компоненты системы

### 1. Presentation Layer (FastAPI)
- **Ответственность**: Обработка HTTP запросов/ответов
- **Технологии**: FastAPI, Pydantic
- **Функции**:
  - Маршрутизация API endpoints
  - Валидация входных данных
  - Обработка ошибок
  - Автоматическая документация

### 2. Business Logic Layer
- **Ответственность**: Бизнес-логика приложения
- **Компоненты**:
  - Сервисы обработки подписок
  - Логика расчета стоимости
  - Фильтрация и агрегация данных

### 3. Data Access Layer
- **Ответственность**: Работа с базой данных
- **Технологии**: SQLAlchemy ORM
- **Функции**:
  - ORM маппинг
  - Транзакции
  - Индексация
  - Оптимизация запросов

### 4. Database Layer
- **Ответственность**: Хранение данных
- **Технология**: PostgreSQL
- **Особенности**:
  - UUID первичные ключи
  - Индексы для производительности
  - Триггеры для автоматических полей

## 🔧 Технические решения

### Валидация данных
```python
# Custom тип для валидации даты MM-YYYY
class MMYYYYDate(str):
    @classmethod
    def validate(cls, v):
        if not re.match(r'^\d{2}-\d{4}$', v):
            raise ValueError('Date must be in MM-YYYY format')
        return v
```

### Логирование
```python
# Настройка с ротацией логов
logger = logging.getLogger("subscription_service")
handler = RotatingFileHandler(
    "app.log",
    maxBytes=10*1024*1024,  # 10MB
    backupCount=5
)
```

### Обработка ошибок
```python
@app.exception_handler(HTTPException)
async def http_exception_handler(request, exc):
    logger.error(f"HTTP error: {exc.detail}")
    return JSONResponse(
        status_code=exc.status_code,
        content={"detail": exc.detail}
    )
```

## 📊 Модель данных

### Таблица subscriptions

| Поле | Тип | Ограничения | Индексы |
|------|-----|-------------|---------|
| id | UUID | PRIMARY KEY | - |
| service_name | VARCHAR(255) | NOT NULL | INDEX |
| price | INTEGER | NOT NULL, CHECK > 0 | - |
| user_id | UUID | NOT NULL | INDEX |
| start_date | DATE | NOT NULL | INDEX |
| end_date | DATE | NULLABLE | INDEX |
| created_at | TIMESTAMP | DEFAULT NOW() | - |
| updated_at | TIMESTAMP | DEFAULT NOW() | - |

### Индексы
- `idx_subscriptions_user_id` - для фильтрации по пользователю
- `idx_subscriptions_service_name` - для поиска по сервису
- `idx_subscriptions_start_date` - для временных запросов
- `idx_subscriptions_end_date` - для временных запросов

## 🔒 Безопасность

### Валидация входных данных
- Pydantic схемы для всех endpoints
- Проверка форматов (UUID, даты, цены)
- Ограничения на длину строк

### Защита от инъекций
- Использование ORM (SQLAlchemy)
- Параметризованные запросы
- Автоматическое экранирование

## 📈 Производительность

### Оптимизации
- Индексация ключевых полей
- Ленивая загрузка связанных данных
- Пагинация результатов
- Кэширование метаданных

### Мониторинг
- Логирование всех операций
- Отслеживание времени выполнения запросов
- Health check endpoint

## 🔄 Миграции базы данных

### Alembic миграции
```bash
# Создание новой миграции
alembic revision --autogenerate -m "Description"

# Применение миграций
alembic upgrade head

# Откат миграций
alembic downgrade -1
```

### SQL миграции
Хранятся в директории `migrations/` для ручного применения.

## 🧪 Тестирование

### Unit тесты
- Тестирование каждого endpoint
- Проверка валидации данных
- Тестирование бизнес-логики

### Integration тесты
- Тестирование взаимодействия с БД
- Проверка комплексных сценариев

## 📦 Контейнеризация

### Docker образ
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Docker Compose
- Сервис приложения
- Сервис базы данных
- Совместные сети и volumes

## 🚀 Deployment

### Локальное развертывание
1. Установка зависимостей
2. Настройка PostgreSQL
3. Применение миграций
4. Запуск приложения

### Docker развертывание
```bash
docker-compose up --build
```

### Production considerations
- Настройка reverse proxy (nginx)
- SSL сертификаты
- Мониторинг и alerting
- Backup стратегии