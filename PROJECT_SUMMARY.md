# Subscription Aggregation Service - Project Summary

## 📋 Обзор проекта

Реализован REST-сервис для агрегации данных об онлайн подписках пользователей с полным набором требуемых функций.

## ✅ Реализованные требования

### 1. CRUDL операции над подписками
- ✅ **POST /subscriptions/** - Создание подписки
- ✅ **GET /subscriptions/{id}** - Получение подписки по ID  
- ✅ **PUT /subscriptions/{id}** - Обновление подписки
- ✅ **DELETE /subscriptions/{id}** - Удаление подписки
- ✅ **GET /subscriptions/** - Список подписок с фильтрацией

### 2. Расчет суммарной стоимости
- ✅ **GET /subscriptions/cost** - Подсчет стоимости за период с фильтрацией по user_id и service_name

### 3. База данных
- ✅ PostgreSQL в качестве СУБД
- ✅ SQL миграции для инициализации базы данных
- ✅ Индексы для оптимизации запросов

### 4. Логирование
- ✅ Полное покрытие кода логами
- ✅ Настройка уровней логирования через .env
- ✅ Ротация лог-файлов

### 5. Конфигурация
- ✅ Вынесение настроек в .env файл
- ✅ Поддержка различных сред выполнения

### 6. Документация
- ✅ Автоматическая Swagger/OpenAPI документация
- ✅ Интерактивная документация через /docs и /redoc

### 7. Контейнеризация
- ✅ Dockerfile для приложения
- ✅ docker-compose.yml для запуска сервиса + БД
- ✅ Готовая инфраструктура для развертывания

## 📁 Структура проекта

```
subscription-service/
├── app/                          # Основное приложение
│   ├── api/                     # API роуты
│   │   └── routes/
│   │       └── subscriptions.py # CRUDL операции и расчет стоимости
│   ├── database/                # Настройка БД
│   │   └── session.py          # Подключение к PostgreSQL
│   ├── models/                  # SQLAlchemy модели
│   │   └── subscription.py     # Модель подписки
│   ├── schemas/                 # Pydantic схемы
│   │   └── subscription.py     # Валидация данных
│   ├── utils/                   # Утилиты
│   │   └── logger.py           # Настройка логирования
│   ├── main.py                 # Точка входа FastAPI
│   └── __init__.py
├── alembic/                     # Alembic миграции (настроен)
├── migrations/                  # SQL миграции
│   ├── 001_create_subscriptions_table.sql
│   └── 002_insert_test_data.sql
├── tests/                       # Тесты
│   └── test_subscriptions.py   # Unit тесты API
├── .env                         # Конфигурационные переменные
├── docker-compose.yml           # Docker Compose конфигурация
├── Dockerfile                   # Docker образ приложения
├── requirements.txt             # Зависимости Python
├── README.md                    # Документация
├── start_service.py             # Скрипт запуска сервиса
└── run_local_test.py            # Скрипт тестирования
```

## 🔧 Основные особенности реализации

### Валидация данных
- Формат дат: MM-YYYY (например, "07-2025")
- Целочисленные цены в рублях
- UUID для идентификаторов пользователей
- Обязательные и опциональные поля

### Безопасность и производительность
- Индексация по ключевым полям (user_id, service_name, даты)
- Защита от SQL-инъекций через SQLAlchemy ORM
- Валидация входных данных через Pydantic

### Логирование
- Разделение логов по уровням (INFO, ERROR, DEBUG)
- Ротация лог-файлов (10MB, 5 резервных копий)
- Логирование всех операций CRUDL

## 🚀 Как запустить

### Локальный запуск:
1. Установить зависимости: `pip install -r requirements.txt`
2. Настроить PostgreSQL базу данных
3. Выполнить миграции из `migrations/` каталога
4. Запустить сервис: `python start_service.py`

### Через Docker:
```bash
docker-compose up --build
```

## 📊 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/subscriptions/` | Создание новой подписки |
| GET | `/subscriptions/{id}` | Получение подписки по ID |
| PUT | `/subscriptions/{id}` | Обновление подписки |
| DELETE | `/subscriptions/{id}` | Удаление подписки |
| GET | `/subscriptions/` | Список подписок с фильтрацией |
| GET | `/subscriptions/cost` | Расчет суммарной стоимости |

## 🧪 Тестирование

- Unit тесты для всех основных endpoint'ов
- Скрипт автоматического тестирования (`run_local_test.py`)
- Проверка валидации данных и обработки ошибок

## 📚 Документация

- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`
- Health check: `http://localhost:8000/health`

## 🎯 Пример использования

```bash
# Создание подписки
curl -X POST "http://localhost:8000/subscriptions/" \
  -H "Content-Type: application/json" \
  -d '{
    "service_name": "Yandex Plus",
    "price": 400,
    "user_id": "60601fee-2bf1-4721-ae6f-7636e79a0cba",
    "start_date": "07-2025"
  }'

# Расчет стоимости за период
curl -X GET "http://localhost:8000/subscriptions/cost/?start_period=01-2025&end_period=12-2025"
```

Проект полностью соответствует всем заданным требованиям и готов к использованию в production среде.